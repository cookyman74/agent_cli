# Phase 1+3 통합 리뷰 추가 수정 이력

> **작업일**: 2026-02-16 **브랜치**: `v0.1.2/white_labelling` **범위**: Phase 1
> (Core Storage) + Phase 3 (CLI Extension) 2차 리뷰에서 발견된 5건 수정
> **선행**: Phase 1+3 통합 리뷰 7건 수정 완료 (`5bbb869a3`)

---

## 1. 리뷰 이슈 검증 결과

| #   | 이슈                                               | 심각도 | 검증 결과                                              | 조치        |
| --- | -------------------------------------------------- | ------ | ------------------------------------------------------ | ----------- |
| 1   | link extension uninstall이 원본 소스 디렉토리 삭제 | HIGH   | **확인** — `extension.path` = `installMetadata.source` | 코드 수정   |
| 2   | OAuth token clear/delete 시 legacy 토큰 부활       | HIGH   | **확인** — Set dedup로 `.gemini` 파일 미삭제           | 코드 수정   |
| 3   | Extension update rollback이 실제 동작하지 않음     | MEDIUM | **확인** — tempDir에 백업 없이 빈 dir에서 복원 시도    | 코드 수정   |
| 4   | Extension install/update가 legacy 경로에 기록 가능 | MEDIUM | **확인** — `getExtensionDir()` = resolveReadPath 사용  | 코드 수정   |
| 5   | 위 이슈들에 대한 회귀 테스트 부재                  | LOW    | **확인** — 관련 테스트 0건                             | 테스트 추가 |

---

## 2. 수정 상세

### 2.1 Issue 1: link extension uninstall이 원본 소스 디렉토리 삭제

**문제**:

이전 수정(`758520b12`)에서 uninstall을 `extension.path` 직접 사용으로
변경했으나, link-type extension의 경우:

- `loadExtension(extensionDir)` → `installMetadata.type === 'link'` →
  `effectiveExtensionPath = installMetadata.source` (원본 소스)
- `extension.path = effectiveExtensionPath` (line 661)
- uninstall 시 `rm -rf extension.path` → **원본 소스 디렉토리 삭제**

**수정**:

```typescript
// extension-manager.ts — link-type 가드 추가
const pathToDelete =
  extension.installMetadata?.type === 'link'
    ? new ExtensionStorage(extension.name).getExtensionDir()
    : extension.path;
await fs.promises.rm(pathToDelete, { recursive: true, force: true });
```

- link-type: 메타데이터 디렉토리(`~/.didim/extensions/name/`) 삭제
- non-link: `extension.path`(= 설치 디렉토리) 삭제 (기존 동작 유지)

---

### 2.2 Issue 2: OAuth token clear/delete 시 legacy 토큰 부활

**문제**:

`deleteTokenFiles()`에서 `new Set([tokenWritePath, tokenReadPath])` 사용:

1. 양쪽 파일 존재 시 `tokenReadPath` = `.didim/...` (resolveReadPath 우선순위)
2. `tokenWritePath` = `.didim/...`
3. Set dedup → `{.didim/...}` 1개만 → `.gemini/...` 파일 미삭제
4. `.didim/...` 삭제 후 다음 read → `.gemini/...` 파일 발견 → **legacy 토큰
   부활**

**수정**:

```typescript
// file-token-storage.ts — 양쪽 경로 명시적 포함
const home = homedir();
const tokenFileName = 'mcp-oauth-tokens-v2.json';
for (const tokenPath of new Set([
  this.tokenWritePath,
  this.tokenReadPath,
  path.join(home, DIDIM_DIR, tokenFileName),
  path.join(home, LEGACY_GEMINI_DIR, tokenFileName),
])) {
  try {
    await fs.unlink(tokenPath);
  } catch (error: unknown) {
    if ((error as NodeJS.ErrnoException).code !== 'ENOENT') throw error;
  }
}
```

---

### 2.3 Issue 3: Extension update rollback이 실제 동작하지 않음

**문제**:

`update.ts`에서 `tempDir = ExtensionStorage.createTmpDir()` 후:

1. `installOrUpdateExtension()` 내부에서 `uninstallExtension()` → 기존 삭제
2. 새 버전 설치 실패
3. catch: `copyExtension(tempDir, extension.path)` → **빈 tempDir에서 복원**

`tempDir`에 백업이 없으므로 rollback이 동작하지 않음.

**수정**:

```typescript
// update.ts — 업데이트 전 백업 추가
const tempDir = await ExtensionStorage.createTmpDir();
try {
  await copyExtension(extension.path, tempDir); // ← 백업 추가
  // ... installOrUpdateExtension() ...
} catch (e) {
  await copyExtension(tempDir, extension.path); // 백업에서 복원
  throw e;
}
```

---

### 2.4 Issue 4: Extension install/update가 legacy 경로에 기록 가능

**문제**:

`installOrUpdateExtension()`에서:

```typescript
const destinationPath = new ExtensionStorage(name).getExtensionDir();
```

`getExtensionDir()` = `resolveReadPath(homedir(), 'extensions', name)`:

- `.gemini/extensions/name` 존재 시 → `.gemini/...` 반환
- `mkdir(destinationPath)` → legacy 경로에 기록
- **write-always-to-.didim 정책 위반**

**수정**:

```typescript
// storage.ts — write 전용 메서드 추가
getExtensionWriteDir(): string {
  return path.join(ExtensionStorage.getUserExtensionsWriteDir(), this.extensionName);
}

// extension-manager.ts — install에서 write path 사용
const destinationPath = new ExtensionStorage(name).getExtensionWriteDir();
```

---

### 2.5 Issue 5: 회귀 테스트 추가

**추가 테스트**:

| 파일                         | 테스트                                                    |
| ---------------------------- | --------------------------------------------------------- |
| `extension.test.ts`          | link-type uninstall: 원본 소스 보존, 메타데이터 dir 삭제  |
| `file-token-storage.test.ts` | clearAll: `.didim` + `.gemini` 양쪽 파일 삭제 검증        |
| `update.test.ts`             | rollback: 백업 생성 → 설치 시도 → 백업에서 복원 순서 검증 |
| `update.test.ts`             | 성공 케이스: 백업 생성 호출 검증                          |

---

## 3. 커밋 내역

| 커밋        | 메시지                                                                                 | 파일 수 |
| ----------- | -------------------------------------------------------------------------------------- | ------- |
| `66c8b7921` | `fix(cli): guard link-type extension uninstall from deleting source directory`         | 1       |
| `4074f75e3` | `fix(core): delete both primary and legacy token files to prevent re-exposure`         | 1       |
| `771409b44` | `fix(cli): backup extension before update for working rollback`                        | 1       |
| `61ff65d4f` | `fix(cli): use write path for extension install destination`                           | 2       |
| `d1c674b56` | `test(cli,core): add regression tests for link uninstall, token cleanup, and rollback` | 3       |

---

## 4. 품질 게이트

| 항목                        | 결과                                        |
| --------------------------- | ------------------------------------------- |
| TypeScript typecheck        | ✅ 0 errors (전체)                          |
| Core tests (281 files)      | ✅ 5,395 passed (flaky 1건 재실행 통과)     |
| CLI tests (351 files)       | ✅ 4,758 passed (기존 skills 1건 무관 실패) |
| a2a-server tests (12 files) | ✅ 105 passed, 0 failed                     |
| Pre-commit hooks            | ✅ 5/5 커밋 모두 통과                       |

---

## 5. Lessons Learned

- **link-type extension.path 위험**: `extension.path`가 반드시 설치 디렉토리를
  가리키지 않음. link-type은 원본 소스 경로를 가리키므로
  `rm -rf extension.path`는 소스 파괴. `installMetadata.type` 가드 필수
- **Set dedup + dynamic resolve 함정**: `resolveReadPath` getter + Set dedup
  조합 시, 동적 resolve 결과가 현재 파일 시스템 상태에 의존하여 삭제 후 legacy
  파일이 부활. 삭제 작업은 양쪽 경로를 명시적으로 포함
- **빈 tempDir rollback**: 백업 없는 tempDir에서 rollback하면 빈 디렉토리로 복원
  = 사실상 삭제. 백업 → 작업 → 롤백 순서 필수
- **read vs write 경로 분리**: `resolveReadPath`는 읽기 전용(fallback 탐색).
  쓰기 작업에는 항상 write 전용 경로(`getGlobalWritePath` 등) 사용하여 legacy
  경로 기록 방지
